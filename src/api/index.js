import latest from '../mockups/latest.json';
import symbols from '../mockups/symbols.json';

const getLatest = () => {
  return latest;
}

const getSymbols = () => {
  return symbols;
}
export {
  getLatest,
  getSymbols
}